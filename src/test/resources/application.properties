
# Application.
server.port=29000
server.context-path=
spring.application.name=persistence

# No security.
security.ignored=/metrics,/check,/install/model/data

# Inherited configuration.
org.coldis.configuration.common-enabled=true
org.coldis.configuration.aspectj-enabled=false

# Datasource.
org.coldis.configuration.persistence.jpa.base-package=org.coldis.library.test.persistence
spring.datasource.url=jdbc:postgresql://localhost:25000/persistence
spring.datasource.username=persistence
spring.datasource.password=persistence
spring.jpa.hibernate.ddl-auto=create-drop

